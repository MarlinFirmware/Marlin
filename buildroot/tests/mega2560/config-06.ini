#
# Marlin Firmware
# Test: mega2560/config-06.ini
#
# E Axis Only | DOGM MarlinUI
#

[config:base]
default_axis_steps_per_unit              = { 500 }
default_max_feedrate                     = { 25 }
default_max_acceleration                 = { 10000 }
manual_feedrate                          = { 4*60 }
axis_relative_modes                      = { false }
homing_bump_mm                           = {}
homing_bump_divisor                      = {}
homing_feedrate_mm_m                     = {}
reprap_discount_full_graphic_smart_controller = on
autotemp                                 = on
autotemp_proportional                    = on
x_driver_type                            = off
y_driver_type                            = off
z_driver_type                            = off
